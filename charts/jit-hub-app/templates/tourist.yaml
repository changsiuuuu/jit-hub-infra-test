apiVersion: apps/v1
kind: Deployment
metadata:
  name: tourist-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tourist-db
  template:
    metadata:
      labels:
        app: tourist-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_DB
              value: tourist_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: tourist-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: tourist-db
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tourist-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tourist-service
  template:
    metadata:
      labels:
        app: tourist-service
    spec:
      containers:
        - name: tourist-service
          image: "{{ .Values.tourist.image.repository }}:{{ .Values.tourist.image.tag }}"
          imagePullPolicy: IfNotPresent
          env:
            - name: JITHUB_LOCATION
              value: K8S-DEV
            - name: JITHUB_DATABASE_URL
              value: postgresql://postgres:postgres@tourist-db:5432/tourist_db
            - name: JITHUB_AUTH_SERVICE_URL
              value: http://auth-service:{{ .Values.auth.service.port }}
          ports:
            - containerPort: {{ .Values.tourist.service.port }}
---
apiVersion: v1
kind: Service
metadata:
  name: tourist-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.tourist.service.port }}
      targetPort: {{ .Values.tourist.service.port }}
  selector:
    app: tourist-service
