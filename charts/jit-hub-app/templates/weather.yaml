apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weather-db
  template:
    metadata:
      labels:
        app: weather-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          env:
            - name: POSTGRES_DB
              value: weather_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: weather-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: weather-db
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: weather-service
  template:
    metadata:
      labels:
        app: weather-service
    spec:
      containers:
        - name: weather-service
          image: "{{ .Values.weather.image.repository }}:{{ .Values.weather.image.tag }}"
          imagePullPolicy: IfNotPresent
          env:
            - name: JITHUB_LOCATION
              value: K8S-DEV
            - name: JITHUB_DATABASE_URL
              value: postgresql://postgres:postgres@weather-db:5432/weather_db
            - name: JITHUB_AUTH_SERVICE_URL
              value: http://auth-service:{{ .Values.auth.service.port }}
          ports:
            - containerPort: {{ .Values.weather.service.port }}
---
apiVersion: v1
kind: Service
metadata:
  name: weather-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.weather.service.port }}
      targetPort: {{ .Values.weather.service.port }}
  selector:
    app: weather-service
