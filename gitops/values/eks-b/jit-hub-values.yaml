# gitops/values/eks-b/jit-hub-values.yaml
# Umbrella Chart (Subchart) 구조에 맞게 작성된 EKS-A (Active) 환경 설정

ingressRule:
  namespace: "jit-hub"
  host: "leechs.shop" # 필요시 eks-a 전용 도메인으로 변경 가능

gateway:
  replicaCount: 1
  image:
    repository: "changsiuuuu/jit-hub-gateway"
    tag: "latest"

auth:
  replicaCount: 1
  image:
    repository: "changsiuuuu/auth-service"
    tag: "latest"
  db:
    enabled: false
    host: "auth-db.internal"

weather:
  replicaCount: 1
  image:
    repository: "changsiuuuu/weather-service"
    tag: "latest"
  db:
    enabled: false
    host: "weather-db.internal"

traffic:
  replicaCount: 1
  image:
    repository: "changsiuuuu/traffic-service"
    tag: "latest"
  db:
    enabled: false
    host: "traffic-db.internal"

tourist:
  replicaCount: 1
  image:
    repository: "changsiuuuu/tourist-service"
    tag: "latest"
  db:
    enabled: false
    host: "tourist-db.internal"
